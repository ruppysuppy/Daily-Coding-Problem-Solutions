PROBLEM 11:

Implement an autocomplete system. 
That is, given a query string s and a set of all possible query strings, return all strings in the set that have s as a prefix.


EXAMPLE:

input = ['dog', 'deer', 'deal'], 'de' 
output = [deer, deal]